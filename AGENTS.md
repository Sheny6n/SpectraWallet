When a request could reasonably be interpreted as applying beyond the immediate screen, file, or flow, first consider whether a global app-wide change would be more coherent.

Before applying a change globally across the app, ask for permission unless the user explicitly requested a global pass.
